# Crawl PubSub Compatibility Module

`spring-modules:crawl-pubsub` no longer contains a separate implementation.
It only re-exports `spring-modules:crawl`.

New projects should depend on `spring-modules:crawl` directly.
